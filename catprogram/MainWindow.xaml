<Window x:Class="catprogram.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CollegeHub"
        Width="1560"
        Height="940"
        MinWidth="1280"
        MinHeight="820"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Border Background="{DynamicResource AppBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrushSoft}"
            BorderThickness="1"
            CornerRadius="24"
            Margin="10"
            Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64" />
                <RowDefinition Height="*" />
                <RowDefinition Height="34" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#10192B"
                    CornerRadius="24,24,0,0"
                    BorderBrush="#22324A"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource WindowChromeButtonStyle}" Background="#FF5F57" BorderBrush="#CC4038" Click="MinimizeButton_Click" Content="" Margin="0,0,8,0" />
                        <Button Style="{StaticResource WindowChromeButtonStyle}" Background="#FEBC2E" BorderBrush="#D89B1E" Click="MaximizeButton_Click" Content="" Margin="0,0,8,0" />
                        <Button Style="{StaticResource WindowChromeButtonStyle}" Background="#28C840" BorderBrush="#1A9E31" Click="CloseButton_Click" Content="" />
                        <StackPanel Orientation="Vertical" Margin="12,0,0,0">
                            <TextBlock Text="CollegeHub" FontSize="20" FontWeight="Bold" />
                            <TextBlock Text="Локальная панель управления колледжем" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Background="#132238" CornerRadius="14" Padding="14,8" BorderBrush="#22324A" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse Width="10" Height="10" Fill="{StaticResource AccentBrush}" Margin="0,0,10,0" />
                                <TextBlock Text="{Binding StatusMessage}" FontSize="13" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource GhostButtonStyle}" Content="Обновить" Command="{Binding RefreshCommand}" Margin="0,0,10,0" />
                        <Button Style="{StaticResource GhostButtonStyle}" Content="Выйти" Command="{Binding LogoutCommand}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Авторизация" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12" />
                                <TextBlock Text="Email" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,6" />
                                <TextBox Text="{Binding LoginEmail, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Text="Пароль" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,14,0,6" />
                                <PasswordBox x:Name="LoginPasswordBox" PasswordChanged="LoginPasswordBox_PasswordChanged" />
                                <Button Content="Войти" Command="{Binding LoginCommand}" Margin="0,16,0,0" />
                                <Border Background="#122033" CornerRadius="14" Padding="12" Margin="0,14,0,0" BorderBrush="#22324A" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Доступы по умолчанию" FontWeight="SemiBold" Margin="0,0,0,8" />
                                        <TextBlock Text="admin@collegehub.local / Admin123!" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="teacher@collegehub.local / Teacher123!" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="student@collegehub.local / Student123!" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Сводка" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12" />
                                <UniformGrid Columns="2" Rows="4" Margin="0,0,0,8">
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="0,0,8,8">
                                        <StackPanel>
                                            <TextBlock Text="Пользователи" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{Binding Dashboard.TotalUsers}" FontSize="22" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="8,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="Студенты" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{Binding Dashboard.TotalStudents}" FontSize="22" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="0,8,8,0">
                                        <StackPanel>
                                            <TextBlock Text="Группы" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{Binding Dashboard.TotalGroups}" FontSize="22" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="8,8,0,0">
                                        <StackPanel>
                                            <TextBlock Text="Предметы" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{Binding Dashboard.TotalSubjects}" FontSize="22" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="0,8,8,0">
                                        <StackPanel>
                                            <TextBlock Text="Занятия" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{Binding Dashboard.TotalSchedules}" FontSize="22" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="8,8,0,0">
                                        <StackPanel>
                                            <TextBlock Text="Активные QR" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{Binding Dashboard.ActiveQrSessions}" FontSize="22" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="0,8,8,0">
                                        <StackPanel>
                                            <TextBlock Text="Отметки сегодня" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{Binding Dashboard.TodayAttendance}" FontSize="22" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#122033" CornerRadius="14" Padding="12" Margin="8,8,0,0">
                                        <StackPanel>
                                            <TextBlock Text="БД" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                                            <TextBlock Text="SQLite" FontSize="18" FontWeight="Bold" />
                                        </StackPanel>
                                    </Border>
                                </UniformGrid>

                                <TextBlock Text="Путь к файлу БД" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,4" />
                                <TextBox Text="{Binding Dashboard.DatabasePath}" IsReadOnly="True" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Быстрый QR" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12" />
                                <TextBlock Text="Последний код" Foreground="{StaticResource TextMutedBrush}" FontSize="12" />
                                <TextBox Text="{Binding LatestQrCode}" IsReadOnly="True" Margin="0,6,0,12" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Width="90" Text="{Binding ExpiresMinutes, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" />
                                    <Button Content="Сгенерировать" Command="{Binding GenerateQrCommand}" />
                                </StackPanel>
                                <TextBlock Text="Сканирование QR" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,14,0,6" />
                                <TextBox Text="{Binding ScanQrCode, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Content="Отметить посещение" Command="{Binding ScanAttendanceCommand}" Margin="0,10,0,0" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <TabControl>
                        <TabItem Header="Панель">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Margin="2">
                                    <TextBlock Text="Добро пожаловать в CollegeHub" FontSize="24" FontWeight="Bold" Margin="0,0,0,8" />
                                    <TextBlock Text="Здесь собраны локальные сущности, формы управления и журнал посещаемости. Все действия работают через встроенную SQLite-базу и готовы к последующему подключению FastAPI без перестройки интерфейса." FontSize="14" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,16" />

                                    <UniformGrid Columns="3" Margin="0,0,0,16">
                                        <Border Background="#122033" CornerRadius="16" Padding="16" Margin="0,0,12,0">
                                            <StackPanel>
                                                <TextBlock Text="Локальная аутентификация" FontWeight="SemiBold" />
                                                <TextBlock Text="Вход, смена пользователя и выход" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,6,0,0" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#122033" CornerRadius="16" Padding="16" Margin="6,0,6,0">
                                            <StackPanel>
                                                <TextBlock Text="CRUD-операции" FontWeight="SemiBold" />
                                                <TextBlock Text="Пользователи, группы, предметы, расписание" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,6,0,0" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#122033" CornerRadius="16" Padding="16" Margin="12,0,0,0">
                                            <StackPanel>
                                                <TextBlock Text="QR и посещаемость" FontWeight="SemiBold" />
                                                <TextBlock Text="Генерация, активация и фиксация отметок" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,6,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </UniformGrid>

                                    <Border Background="#132238" CornerRadius="16" Padding="16" BorderBrush="#22324A" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="Текущее состояние" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8" />
                                            <TextBlock Text="{Binding CurrentUser.DisplayName, TargetNullValue=Пользователь не авторизован}" FontSize="13" />
                                            <TextBlock Text="{Binding StatusMessage}" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,6,0,0" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <TabItem Header="Пользователи">
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="360" />
                                </Grid.ColumnDefinitions>

                                <DataGrid ItemsSource="{Binding Users}" SelectionChanged="UsersGrid_SelectionChanged" AutoGenerateColumns="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                                        <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*" />
                                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220" />
                                        <DataGridTextColumn Header="Роль" Binding="{Binding RoleName}" Width="120" />
                                        <DataGridTextColumn Header="Группа" Binding="{Binding GroupName}" Width="120" />
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Column="2" Background="#132238" CornerRadius="16" Padding="16" BorderBrush="#22324A" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Карточка пользователя" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                                        <TextBlock Text="ФИО" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBox Text="{Binding NewUserName, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Text="Email" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
                                        <TextBox Text="{Binding NewUserEmail, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Text="Пароль" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
                                        <PasswordBox x:Name="NewUserPasswordBox" PasswordChanged="NewUserPasswordBox_PasswordChanged" />
                                        <TextBlock Text="Роль" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
                                        <ComboBox ItemsSource="{Binding Roles}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding NewUserRoleId, Mode=TwoWay}" />
                                        <TextBlock Text="Группа" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
                                        <ComboBox ItemsSource="{Binding Groups}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding NewUserGroupId, Mode=TwoWay}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                                            <Button Content="Создать" Command="{Binding CreateUserCommand}" Margin="0,0,10,0" />
                                            <Button Content="Обновить" Command="{Binding UpdateUserCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0" />
                                            <Button Content="Удалить" Command="{Binding DeleteUserCommand}" Style="{StaticResource DangerButtonStyle}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem Header="Группы">
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="340" />
                                </Grid.ColumnDefinitions>

                                <DataGrid ItemsSource="{Binding Groups}" SelectionChanged="GroupsGrid_SelectionChanged" AutoGenerateColumns="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                                        <DataGridTextColumn Header="Курс" Binding="{Binding Course}" Width="80" />
                                        <DataGridTextColumn Header="Отделение" Binding="{Binding Department}" Width="220" />
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Column="2" Background="#132238" CornerRadius="16" Padding="16" BorderBrush="#22324A" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Карточка группы" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                                        <TextBlock Text="Название" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBox Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Text="Курс" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
                                        <TextBox Text="{Binding NewGroupCourse, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Text="Отделение" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
                                        <TextBox Text="{Binding NewGroupDepartment, UpdateSourceTrigger=PropertyChanged}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                                            <Button Content="Создать" Command="{Binding CreateGroupCommand}" Margin="0,0,10,0" />
                                            <Button Content="Обновить" Command="{Binding UpdateGroupCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0" />
                                            <Button Content="Удалить" Command="{Binding DeleteGroupCommand}" Style="{StaticResource DangerButtonStyle}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem Header="Предметы">
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="340" />
                                </Grid.ColumnDefinitions>

                                <DataGrid ItemsSource="{Binding Subjects}" SelectionChanged="SubjectsGrid_SelectionChanged" AutoGenerateColumns="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                                        <DataGridTextColumn Header="Преподаватель" Binding="{Binding TeacherName}" Width="220" />
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Column="2" Background="#132238" CornerRadius="16" Padding="16" BorderBrush="#22324A" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Карточка предмета" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                                        <TextBlock Text="Название" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBox Text="{Binding NewSubjectName, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Text="Преподаватель" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
                                        <ComboBox ItemsSource="{Binding Users}" DisplayMemberPath="FullName" SelectedValuePath="Id" SelectedValue="{Binding NewSubjectTeacherId, Mode=TwoWay}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                                            <Button Content="Создать" Command="{Binding CreateSubjectCommand}" Margin="0,0,10,0" />
                                            <Button Content="Обновить" Command="{Binding UpdateSubjectCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0" />
                                            <Button Content="Удалить" Command="{Binding DeleteSubjectCommand}" Style="{StaticResource DangerButtonStyle}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem Header="Расписание">
                            <Grid Margin="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="16" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <DataGrid Grid.Row="0" ItemsSource="{Binding Schedules}" SelectionChanged="SchedulesGrid_SelectionChanged" AutoGenerateColumns="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                                        <DataGridTextColumn Header="Группа" Binding="{Binding GroupName}" Width="120" />
                                        <DataGridTextColumn Header="Предмет" Binding="{Binding SubjectName}" Width="*" />
                                        <DataGridTextColumn Header="Преподаватель" Binding="{Binding TeacherName}" Width="160" />
                                        <DataGridTextColumn Header="День" Binding="{Binding DayOfWeek}" Width="100" />
                                        <DataGridTextColumn Header="Время" Binding="{Binding StartTime}" Width="90" />
                                        <DataGridTextColumn Header="Кабинет" Binding="{Binding Room}" Width="90" />
                                        <DataGridTextColumn Header="Тип" Binding="{Binding LessonType}" Width="90" />
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Row="2" Background="#132238" CornerRadius="16" Padding="16" BorderBrush="#22324A" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.3*" />
                                            <ColumnDefinition Width="16" />
                                            <ColumnDefinition Width="1*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Карточка занятия" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                                            <UniformGrid Columns="2">
                                                <StackPanel Margin="0,0,8,0">
                                                    <TextBlock Text="Группа" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <ComboBox ItemsSource="{Binding Groups}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding NewScheduleGroupId, Mode=TwoWay}" />
                                                </StackPanel>
                                                <StackPanel Margin="8,0,0,0">
                                                    <TextBlock Text="Предмет" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <ComboBox ItemsSource="{Binding Subjects}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding NewScheduleSubjectId, Mode=TwoWay}" />
                                                </StackPanel>
                                                <StackPanel Margin="0,10,8,0">
                                                    <TextBlock Text="Преподаватель" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <ComboBox ItemsSource="{Binding Users}" DisplayMemberPath="FullName" SelectedValuePath="Id" SelectedValue="{Binding NewScheduleTeacherId, Mode=TwoWay}" />
                                                </StackPanel>
                                                <StackPanel Margin="8,10,0,0">
                                                    <TextBlock Text="Кабинет" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <TextBox Text="{Binding NewRoom, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                                <StackPanel Margin="0,10,8,0">
                                                    <TextBlock Text="Начало" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <TextBox Text="{Binding NewStartTime, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                                <StackPanel Margin="8,10,0,0">
                                                    <TextBlock Text="Конец" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <TextBox Text="{Binding NewEndTime, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                                <StackPanel Margin="0,10,8,0">
                                                    <TextBlock Text="День недели" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <TextBox Text="{Binding NewDay, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                                <StackPanel Margin="8,10,0,0">
                                                    <TextBlock Text="Тип" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                    <TextBox Text="{Binding NewLessonType, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                            </UniformGrid>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock Text="QR-сессии" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="90" Text="{Binding ExpiresMinutes, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" />
                                                <Button Content="Создать QR" Command="{Binding GenerateQrCommand}" />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                <Button Content="Добавить" Command="{Binding CreateScheduleCommand}" Margin="0,0,10,0" />
                                                <Button Content="Обновить" Command="{Binding UpdateScheduleCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0" />
                                                <Button Content="Удалить" Command="{Binding DeleteScheduleCommand}" Style="{StaticResource DangerButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem Header="QR-сессии">
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="360" />
                                </Grid.ColumnDefinitions>

                                <DataGrid ItemsSource="{Binding QrSessions}" SelectionChanged="QrSessionsGrid_SelectionChanged" AutoGenerateColumns="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                                        <DataGridTextColumn Header="Занятие" Binding="{Binding ScheduleLabel}" Width="*" />
                                        <DataGridTextColumn Header="Статус" Binding="{Binding StateText}" Width="110" />
                                        <DataGridTextColumn Header="Действует до" Binding="{Binding ExpiresAt}" Width="160" />
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Column="2" Background="#132238" CornerRadius="16" Padding="16" BorderBrush="#22324A" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Действующая QR-сессия" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                                        <TextBlock Text="QR-код" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBox Text="{Binding LatestQrCode}" IsReadOnly="True" />
                                        <Button Content="Выбрать последнюю" Command="{Binding SelectQrFromLatestCommand}" Margin="0,10,0,0" Style="{StaticResource GhostButtonStyle}" />
                                        <Button Content="Активировать/деактивировать" Command="{Binding ToggleQrStateCommand}" Margin="0,10,0,0" />
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem Header="Посещаемость">
                            <DataGrid ItemsSource="{Binding Attendance}" AutoGenerateColumns="False" SelectionMode="Single">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                                    <DataGridTextColumn Header="Студент" Binding="{Binding UserName}" Width="180" />
                                    <DataGridTextColumn Header="Группа" Binding="{Binding GroupName}" Width="110" />
                                    <DataGridTextColumn Header="Предмет" Binding="{Binding SubjectName}" Width="*" />
                                    <DataGridTextColumn Header="QR" Binding="{Binding QrCodeData}" Width="280" />
                                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100" />
                                    <DataGridTextColumn Header="Время" Binding="{Binding ScannedAtText}" Width="150" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </TabItem>
                    </TabControl>
                </Border>
            </Grid>

            <Border Grid.Row="2" Background="#10192B" BorderBrush="#22324A" BorderThickness="0,1,0,0" CornerRadius="0,0,24,24">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Text="{Binding StatusMessage}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" />
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="SQLite · WPF · локальная база" Foreground="{StaticResource TextMutedBrush}" FontSize="12" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>