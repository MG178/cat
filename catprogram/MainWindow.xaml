<Window x:Class="catprogram.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CollegeHub"
        Width="1540"
        Height="920"
        MinWidth="1260"
        MinHeight="820"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">
    <Border Background="{DynamicResource AppBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrushSoft}"
            BorderThickness="1"
            CornerRadius="24"
            Margin="10"
            Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="58" />
                <RowDefinition Height="*" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#10192B" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,0,0">
                    <Button Style="{StaticResource WindowChromeButtonStyle}" Width="18" Height="18" Background="#FF5F57" BorderBrush="#CC4038" Click="MinimizeButton_Click" Margin="0,0,6,0" />
                    <Button Style="{StaticResource WindowChromeButtonStyle}" Width="18" Height="18" Background="#FEBC2E" BorderBrush="#D89B1E" Click="MaximizeButton_Click" Margin="0,0,6,0" />
                    <Button Style="{StaticResource WindowChromeButtonStyle}" Width="18" Height="18" Background="#28C840" BorderBrush="#1A9E31" Click="CloseButton_Click" />
                    <StackPanel Orientation="Vertical" Margin="12,0,0,0">
                        <TextBlock Text="CollegeHub" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="Локальная панель колледжа" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" Background="#132238" CornerRadius="14" Padding="12,6" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="#22324A" BorderThickness="1">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextMutedBrush}" />
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,18,0">
                    <Button Content="Обновить" Command="{Binding RefreshCommand}" Margin="0,0,10,0" />
                    <Button Content="Выйти" Command="{Binding LogoutCommand}" Style="{StaticResource GhostButtonStyle}" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="Transparent">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Background="Transparent" />
            </Border>

            <Border Grid.Row="2" Background="#10192B" BorderBrush="#22324A" BorderThickness="0,1,0,0">
                <Grid Margin="18,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Text="{Binding StatusMessage}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" />
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="SQLite · WPF · локальная база" Foreground="{StaticResource TextMutedBrush}" FontSize="12" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>