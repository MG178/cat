<Window x:Class="catprogram.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="CollegeHub"
        Width="1560"
        Height="930"
        MinWidth="1280"
        MinHeight="840"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">
    <Border Background="{DynamicResource AppBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrushSoft}"
            BorderThickness="1"
            CornerRadius="24"
            Margin="10"
            Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />
                <RowDefinition Height="*" />
                <RowDefinition Height="28" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#0E1826" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Button Style="{StaticResource WindowChromeButtonStyle}" Content="—" Margin="0,0,6,0" Click="MinimizeButton_Click" />
                    <Button Style="{StaticResource WindowChromeButtonStyle}" Content="▢" Margin="0,0,6,0" Click="MaximizeButton_Click" />
                    <Button Style="{StaticResource WindowChromeButtonStyle}" Content="✕" Click="CloseButton_Click" />
                    <StackPanel Orientation="Vertical" Margin="12,0,0,0">
                        <TextBlock Text="CollegeHub" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="Локальная учебная панель" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1"
                        Background="#132033"
                        CornerRadius="14"
                        Padding="12,6"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        BorderBrush="#22324A"
                        BorderThickness="1">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextMutedBrush}" />
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <Button Content="Обновить" Command="{Binding RefreshCommand}" Margin="0,0,10,0" />
                    <Button Content="Выйти" Command="{Binding LogoutCommand}" Style="{StaticResource GhostButtonStyle}" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="Transparent">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Background="Transparent" />
            </Border>

            <Border Grid.Row="2" Background="#0E1826" BorderBrush="#22324A" BorderThickness="0,1,0,0">
                <Grid Margin="16,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Text="{Binding StatusMessage}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" />
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="SQLite / MVVM" Foreground="{StaticResource TextMutedBrush}" FontSize="12" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
