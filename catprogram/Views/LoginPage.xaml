<Page x:Class="catprogram.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="Transparent">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.05*" />
            <ColumnDefinition Width="24" />
            <ColumnDefinition Width="0.95*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="CollegeHub" FontSize="30" FontWeight="Bold" />
                    <TextBlock Text="Отдельная форма авторизации без лишнего скролла" FontSize="14" Foreground="{StaticResource TextMutedBrush}" Margin="0,8,0,0" />
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="Email" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBox Text="{Binding LoginEmail, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,12" />
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="Пароль" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                    <PasswordBox x:Name="PasswordBoxControl" PasswordChanged="PasswordBoxControl_PasswordChanged" Margin="0,6,0,16" />
                </StackPanel>

                <Button Grid.Row="3" Content="Войти" Command="{Binding LoginCommand}" HorizontalAlignment="Left" Padding="24,10" />

                <Border Grid.Row="4" Background="#122033" CornerRadius="16" Padding="14" Margin="0,18,0,0" BorderBrush="#22324A" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Тестовые доступы" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <TextBlock Text="admin@collegehub.local / Admin123!" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                        <TextBlock Text="teacher@collegehub.local / Teacher123!" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                        <TextBlock Text="student@collegehub.local / Student123!" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="Секции проекта" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,10" />
                <TextBlock Text="Окно построено на MVVM и переключает отдельные frame-страницы внутри одного shell. Разделы зависят от роли пользователя." FontSize="14" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,20" />

                <UniformGrid Columns="2">
                    <Border Background="#122033" CornerRadius="16" Padding="16" Margin="0,0,10,10"><TextBlock Text="Авторизация" FontWeight="SemiBold" /></Border>
                    <Border Background="#122033" CornerRadius="16" Padding="16" Margin="10,0,0,10"><TextBlock Text="Пользователи" FontWeight="SemiBold" /></Border>
                    <Border Background="#122033" CornerRadius="16" Padding="16" Margin="0,10,10,0"><TextBlock Text="Расписание" FontWeight="SemiBold" /></Border>
                    <Border Background="#122033" CornerRadius="16" Padding="16" Margin="10,10,0,0"><TextBlock Text="QR и посещаемость" FontWeight="SemiBold" /></Border>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
